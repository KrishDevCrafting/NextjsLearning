"use client";
import React from "react";

const Explorebtn = () => {
  return (
    <button
      className="mt-7 mx-auto"
      id="explore-btn"
      type="button"
      onClick={() => alert("Hello Dear!")}
    >
      Explore button
    </button>
  );
};

export default Explorebtn;

// kasnd
